#include "menu/menu.h"

// Milestone 0 stub. The real body arrives with the base/renderer/submenu system
// (Tasks 8-15): build() initialises ui vars + registers the demo submenu, tick()
// polls the open bind and drives base::update() each frame.
namespace menu {
    void build() {}
    void tick() {}
}
